// line that starts hash is telling its a preprocessor
// if you do #include <filename> then preprocessor copies
// contents to this filename
// iostream contents for input and output streams
#include <iostream>

using namespace std;

// program execution begins from here
int main()
{
    // displays on the output screen
    // two << sign indicates that characters are being pushed
    // to the output stream

    cout << "Hello World" << endl;

    return 0;
}